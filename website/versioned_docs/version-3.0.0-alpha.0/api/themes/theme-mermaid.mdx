---
sidebar_position: 5
slug: /api/themes/@ianaio/theme-mermaid
---

# 📦 theme-mermaid

This theme provides a `@theme/Mermaid` component that is powered by [mermaid](https://mermaid-js.github.io/). You can read more on [diagrams](../../guides/markdown-features/markdown-features-diagrams.mdx) documentation.

```bash npm2yarn
npm install --save @ianaio/theme-mermaid
```

## Configuration {#configuration}

```js title="ianaio.config.js"
module.exports = {
  themes: ['@ianaio/theme-mermaid'],
  // In order for Mermaid code blocks in Markdown to work,
  // you also need to enable the Remark plugin with this option
  markdown: {
    mermaid: true,
  },
};
```
